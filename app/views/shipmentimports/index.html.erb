<p id="notice"><%= notice %></p>

<h1>Shipmentimports</h1>

<table>
  <thead>
    <tr>
      <th>Shipments</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @shipmentimports.each do |shipmentimport| %>
      <tr>
        <td><%= shipmentimport.shipments %></td>
        <td><%= shipmentimport.user_id %></td>
        <td><%= link_to 'Show', shipmentimport %></td>
        <td><%= link_to 'Edit', edit_shipmentimport_path(shipmentimport) %></td>
        <td><%= link_to 'Destroy', shipmentimport, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Shipmentimport', new_shipmentimport_path %>
