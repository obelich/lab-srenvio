<p id="notice"><%= notice %></p>

<h1>Transportistas</h1>

<%= link_to 'Nuevo Trnasportista', new_carrier_path, class: 'btn btn-primary', role: 'button' %>
<br /><br />

<table class="table">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Descripción</th>
      <th>Acciones</th>
    </tr>
  </thead>

  <tbody>
    <% @carriers.each do |carrier| %>
      <tr>
        <td><%= carrier.name %></td>
        <td><%= carrier.description %></td>
        <td>
          <%= link_to '<i class="fas fa-glasses fa-lg"></i>'.html_safe, carrier, class: 'btn btn-secondary' %>
          <%= link_to '<i class="fas fa-edit fa-lg"></i>'.html_safe, edit_carrier_path(carrier), class: 'btn btn-success' %>
          <%= link_to '<i class="fas fa-trash fa-lg"></i>'.html_safe, carrier, method: :delete, data: { confirm: 'Estas seguro?' }, class: 'btn btn-danger' %>

        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= paginate @carriers, theme: 'twitter-bootstrap-4' %>
<br>


